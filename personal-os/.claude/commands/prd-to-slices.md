# PRD To Slices

Use `.agents/skills/prd-to-slices/SKILL.md` if present.
Fallback: `memory/skills/prd-to-slices/skill.md`.

Turn a Build PRD into a vertical slice board for Conductor workspaces.

Write it to `.context/builds/{feature-slug}/slices.md` and generate the HTML board.

Do not write application code.

$ARGUMENTS
